# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do

  watch(%r{.+\.(css|js|html)})

end

guard 'sass', :input => 'sass', :output => 'css' , :style => :expanded, :compass => true
